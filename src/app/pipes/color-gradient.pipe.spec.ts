import { ColorGradientPipe } from './color-gradient.pipe';

describe('ColorGradientPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorGradientPipe();
    expect(pipe).toBeTruthy();
  });
});
